

## Update Namaz Card Navigation to Calendar Page

**`src/pages/Dashboard.tsx`** — Change the `onClick` handler on the TimeOfDayCard wrapper from navigating to `/namaz` to `/calendar`.

Line ~131: `navigate('/namaz')` → `navigate('/calendar')`

